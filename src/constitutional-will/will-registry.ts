/**
 * AZMA OS — THE CONSTITUTIONAL WILL
 * The Constitutional Will Registry
 * Construction Campaign
 *
 * A pure, declarative record of this module's own pipeline stages —
 * documentation-as-code, matching the pattern already used by every
 * prior phase's own top-level registry.
 */

export const CONSTITUTIONAL_WILL_PIPELINE = [
  { stage: 'Receive', description: 'Pull, on demand, from Constitutional Reception\'s own Reception Queue — never from the Nervous System Bus, never from any organ directly.' },
  { stage: 'Evaluate', description: 'Gate each reception on two already-established facts: was it marked attention-worthy, and did its expression preserve constitutional dignity — never re-derived, only read.' },
  { stage: 'Form', description: 'Produce a ConstitutionalIntention carrying the reception\'s own unmodified statement, traceable back to its sourceReceptionId.' },
  { stage: 'Hold', description: 'Readiness is permanent and terminal: \'formed\'. No function anywhere transitions, executes, or notifies from an intention.' },
] as const;
