/**
 * AZMA OS — FAITHFUL CONSTITUTIONAL LISTENING
 * Public API — import from here, never directly from the individual
 * files.
 *
 * Registry Entry IV — The Engine of Faithful Constitutional Listening.
 * See types.ts and listening-composer.ts for the full constitutional
 * disclosure of what this module is (a faithful, minimal preparation of
 * TongueIntent from THE CREATOR PRESENCE) and is not (an owner of
 * TongueIntent, a consumer of Conversation Continuity, or a producer of
 * the Imperial Voice).
 */

export type { ConstitutionalListeningCertification } from './types';

export { prepareTongueIntent } from './listening-composer';

export {
  verifyFaithfullyPreservesCreatorPresenceFields,
  verifyNeverModifiesCreatorPresence,
  verifyNeverRequiresContinuityOrPriorTurns,
  verifyPlaceholderIsDocumentedAsTemporaryNotConstitutional,
  verifyProducesOnlyAFaithfulTongueIntent,
  verifyNoAuthorityExercised,
} from './certification';

export { getConstitutionalListeningCertificationReport } from './queries';
