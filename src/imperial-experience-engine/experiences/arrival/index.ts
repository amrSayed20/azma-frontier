/**
 * AZMA OS — THE IMPERIAL EXPERIENCE ENGINE (IXE)
 * The Arrival Experience — Public API
 *
 * Registered as mounted at '/' only (see engine/experience-registry.ts)
 * and must never render elsewhere.
 */

export { ArrivalExperience } from './ArrivalExperience';
