/**
 * AZMA OS — THE IMPERIAL EXPERIENCE ENGINE (IXE)
 * The Registration Gateway — Public API
 *
 * Registered as mounted at '/signup' only (see engine/experience-registry.ts).
 */

export { SignupExperience } from './SignupExperience';
