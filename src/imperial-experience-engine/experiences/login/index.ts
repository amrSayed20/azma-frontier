/**
 * AZMA OS — THE IMPERIAL EXPERIENCE ENGINE (IXE)
 * The Recognition Gateway — Public API
 *
 * Registered as mounted at '/login' only (see engine/experience-registry.ts).
 */

export { LoginExperience } from './LoginExperience';
