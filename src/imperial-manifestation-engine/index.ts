/**
 * AZMA OS — THE IMPERIAL MANIFESTATION ENGINE (IME)
 * Public API — import from here, never directly from the individual
 * files.
 */

export type { PresentationFamily, PresentationSpec } from './types';
export { manifest } from './manifest';
export {
  IME_CONSTITUTIONAL_PURPOSE,
  IME_RESPONSIBILITIES,
  IME_NON_RESPONSIBILITIES,
  IME_PRESENTATION_CONSUMERS,
  IME_EXTENSION_RULE,
} from './CONSTITUTIONAL_CONTRACT';
